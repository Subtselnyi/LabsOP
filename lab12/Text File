Рассмотрим дробь 1/n, n>1. Как известно, цифры, в её десятичной

записи начиная с некоторого места повторяются. Минимальную по длине

повторяющуюся (без промежутков) часть называют периодом. Минимальную

по длине часть после запятой, которая не входит ни в один период, называют

предпериодом. Например: n=28: 1/28=0,03(571428), предпериод '03', длина 2,

период '571428', длина 6.

Используя макросы обработки переменного числа параметров найти длину предпериода 
и длину периода у каждой дроби.

1 Початок
2 Введення n
3 Виклик Макросу
3.1 Знаходження довжини періоду
3.2 Знаходження довжини преперіоду
4.Кінець















#include <stdio.h>
#include <iostream>
#define EMPTY
#include <stdarg.h>
using namespace std;

void CHECK2(int k, ...)
{
  va_list args;
  va_start(args,k);
  for (int i=0;i<k;i++){
    char s[10]= va_arg(args,char []);
    char s1[6]
    for  (int j=2;j<10;j++){
        if (s[j]=='\0') break;
        s1[j-2]=s[j];
    }
    int n= atoi(s1[0]);
    cout<<"1/"<<n<<endl;
    int l=1, r = 1; \
  int c, k, b,f=0; \
  while (l != (n + 1)) { r = ((10 * r) % n); l++;} \
  c = r; \
  r = (10 * r) % n; \
  k = 1; \
  while (r != c) { r = (10 * r) % n; k++; } \
  while (l != c) { l = (10 * l) % n; f++; } \
  if (r) cout << "Max period " << k << endl; else cout<< "No period"<<endl<<endl; \
  if (r) cout<<"Pre period "<<k-f<<endl<<endl;
  }

}


int main() {

    CHECK2(2, "1/8" ,"1/3");

}
